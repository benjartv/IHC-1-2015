<%= form_for(@tipo_elemento) do |f| %>
  <% if @tipo_elemento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_elemento.errors.count, "error") %> prohibited this tipo_elemento from being saved:</h2>

      <ul>
      <% @tipo_elemento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipoElemento_nombre %><br>
    <%= f.text_field :tipoElemento_nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
